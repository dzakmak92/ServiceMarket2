# Dependencies for the Vercel serverless function ONLY.
#
# Deliberately much smaller than backend/requirements.txt. Vercel caps an
# unzipped function at 250 MB, and pandas + numpy alone are ~80 MB — they are
# used only by tools/ and tests/, never by the app, so they stay out.
#
# Also absent, on purpose:
#   motor / pymongo        no MongoDB on Vercel
#   emergentintegrations   being removed
#   litellm                was pinned to a wheel on Emergent's own asset
#                          server, so the build could not be reproduced
#                          anywhere else
fastapi==0.115.6
uvicorn==0.34.0
pydantic[email]==2.10.5
python-dotenv==1.0.1
python-multipart==0.0.20

asyncpg==0.30.0

PyJWT==2.10.1
bcrypt==4.2.1

httpx==0.28.1

reportlab==4.5.1
segno==1.6.6
pillow==12.2.0
